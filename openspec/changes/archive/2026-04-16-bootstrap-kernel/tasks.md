## 1. 工程骨架

- [x] 1.1 为仓库增加根级 Maven 聚合工程。
- [x] 1.2 将工程目标命名统一到 `taskgraph`，明确根工程坐标为 `io.github.caojiantao.taskgraph:taskgraph`。
- [x] 1.3 定义当前模块布局为 `taskgraph-kernel`。
- [x] 1.4 创建独立的 `taskgraph-kernel` 模块，避免与 Spring 耦合。
- [x] 1.5 在 README 中标记现有实现为原型，并说明 `taskgraph` 内核的演进方向。

## 2. 定义期领域模型

- [x] 2.1 定义 `TaskDefinition`，包含任务标识、依赖列表、处理函数、可选异常回调、可选专属线程池和可选任务级超时。
- [x] 2.2 定义不可变 `TaskGraph`，包含 `graphId`、可选图级线程池、可选图级超时，并封装全部任务定义和图元数据。
- [x] 2.3 定义极简执行请求对象，仅承载图和上下文。
- [x] 2.4 规划 `graph`、`execution`、`result`、`spi`、`validation`、`exception` 与 `internal` 分层包结构。
- [x] 2.5 使用 Lombok 简化定义期模型与请求/结果对象的样板代码。
- [x] 2.6 收敛首版 public API 命名：`TaskGraph`、`TaskDefinition`、`GraphExecutionRequest`、`GraphExecutionResult`、`GraphExecutor`、`TaskHandler`、`TaskErrorHandler`。

## 3. 运行期领域模型

- [x] 3.1 定义图运行时状态枚举与任务运行时状态模型。
- [x] 3.2 定义每次运行独立的运行时状态容器与 `CountDownLatch` 协调模型。
- [x] 3.3 定义结构化 `GraphExecutionResult`，首版仅包含图运行时状态字段 `state`。
- [x] 3.4 定义单一图运行时状态收敛模型，并保持结果模型极简。
- [x] 3.5 将运行时调度、超时和依赖收敛实现明确落入 `internal` 包，不作为稳定 API 暴露。

## 4. 图校验

- [x] 4.1 实现空图拒绝校验。
- [x] 4.2 实现图内重复任务标识校验。
- [x] 4.3 实现缺失依赖校验。
- [x] 4.4 实现循环依赖校验。
- [x] 4.5 让所有非法图都在构建或注册阶段 fail-fast。
- [x] 4.6 实现 `graphId` 唯一性校验，并允许不同图复用同名 `taskId`。

## 5. 调度内核

- [x] 5.1 实现图执行入口 `GraphExecutor`。
- [x] 5.2 实现根任务初始化提交逻辑。
- [x] 5.3 实现图级线程池与任务级覆盖线程池的选择逻辑，并在构建期校验每个任务都能解析出有效线程池。
- [x] 5.4 实现任务完成后的依赖计数递减与下游释放逻辑。
- [x] 5.5 实现图运行时状态收敛，确保任务失败只跳过后继子图，而图级超时后才停止新任务提交。
- [x] 5.6 实现同一图定义多次执行互不污染的运行机制。
- [x] 5.7 保持运行期主路径不重复执行完整 DAG 拓扑校验。

## 6. 失败与超时

- [x] 6.1 实现统一失败语义：失败任务的后继子图跳过、无关分支继续、整图自然收敛后最终保持 `DEGRADED`。
- [x] 6.2 实现任务异常回调。
- [x] 6.3 实现图级超时与 `TIMED_OUT` 结果返回，并在未配置图超时时应用框架默认图超时常量。
- [x] 6.4 在超时时尝试取消未完成任务。
- [x] 6.5 实现框架自定义异常体系，供内部和日志语义使用。
- [x] 6.6 实现任务级超时控制，并在未配置任务超时时应用框架默认任务超时常量，且避免通过 `Future.get(timeout)` 阻塞调度主路径。
- [x] 6.7 引入全局 `timeoutScheduler`，首版采用轻量 `ScheduledThreadPoolExecutor(1)` 承担任务级超时观察。

## 7. 结果与测试

- [x] 7.1 输出极简图结果：结构化对象中的图运行时状态 `state`。
- [x] 7.2 补充正常 DAG 执行测试。
- [x] 7.3 补充多根节点与多依赖汇聚测试。
- [x] 7.4 补充图校验失败测试。
- [x] 7.5 补充任务异常后“后继子图跳过、无关分支继续”的测试。
- [x] 7.6 补充“任务失败后无关分支继续，但整图最终 `DEGRADED`”的测试。
- [x] 7.7 补充任务异常、超时与重复执行测试。
- [x] 7.8 补充“超时后仍返回部分上下文结果”的测试。
- [x] 7.9 补充“主线程同步阻塞等待结果”的测试。

## 8. 文档与交付

- [x] 8.1 编写 `taskgraph` 内核使用文档。
- [x] 8.2 说明当前内核定位，并补充后续扩展方向。
- [x] 8.3 编写工程编码约定，明确禁止新增泛化 `entity` 包、万能 `util` 包、`I` 前缀接口和 `E` 前缀枚举。
